# frozen_string_literal: true

RSpec.describe OmniAuth::Nft do
  it "has a version number" do
    expect(OmniAuth::Nft::VERSION).not_to be nil
  end

  it "does something useful" do
    pending
    expect(false).to eq(true)
  end
end
